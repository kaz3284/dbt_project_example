version: 2

models:
  - name: stg_tpch__nations
    description: "国マスタ"
    columns:
      - name: nation_key
        description: "国キー（主キー）"
        data_tests:
          - unique
          - not_null
      - name: nation_name
        description: "国名"
      - name: region_key
        description: "地域キー。stg_tpch__regions への外部キー"
